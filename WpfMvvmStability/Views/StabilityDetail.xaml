<Page x:Class="WpfMvvmStability.Views.StabilityDetail"
      x:Name="stabilityPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:DV="clr-namespace:System.Windows.Controls.DataVisualization;assembly=System.Windows.Controls.DataVisualization.Toolkit"
      xmlns:DVC="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit"
      xmlns:chartPrimitive="clr-namespace:System.Windows.Controls.DataVisualization.Charting.Primitives;assembly=System.Windows.Controls.DataVisualization.Toolkit"
      mc:Ignorable="d" Background="#F5F7FA"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch">
    <Page.Resources>
        <SolidColorBrush x:Key="PageBg" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#22C55E"/>
        <SolidColorBrush x:Key="AccentRed" Color="#EF4444"/>
        <SolidColorBrush x:Key="AccentAmber" Color="#F59E0B"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderLight" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="RowAlt" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="HeaderBg" Color="#1E3A5F"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="AccentBar" TargetType="Border">
            <Setter Property="Width" Value="5"/>
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="CornerRadius" Value="2"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SummaryValue" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style x:Key="SummaryLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
        </Style>

        <Style x:Key="BtnImport" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnSave" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnClear" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DC2626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DGHeader" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Background" Value="{StaticResource HeaderBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource HeaderBg}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CenterCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="RowBackground" Value="{StaticResource CardBg}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource RowAlt}"/>
            <Setter Property="RowHeight" Value="28"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextBlock.HorizontalAlignment" Value="Center"/>
            <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ChartStyle1" TargetType="{x:Type DVC:Chart}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DVC:Chart}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="0"/>
                                    <RowDefinition Height="250"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0"/>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentControl Content="{TemplateBinding Title}" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <Grid Margin="0,8,0,0" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="2">
                                    <chartPrimitive:EdgePanel x:Name="ChartArea" Style="{TemplateBinding ChartAreaStyle}">
                                        <Grid Style="{TemplateBinding PlotAreaStyle}" Panel.ZIndex="-1"/>
                                        <Border BorderBrush="#B8C2CC" BorderThickness="1" Panel.ZIndex="10"/>
                                    </chartPrimitive:EdgePanel>
                                </Grid>
                                <ContentControl x:Name="Legend" Grid.Row="2" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Top" Content="{TemplateBinding LegendTitle}" FontFamily="Segoe UI" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextPrimary}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer x:Name="scrollViewer1"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Background="{StaticResource PageBg}">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- TOP SUMMARY STRIP -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <UniformGrid Columns="4">
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="GMt" Style="{StaticResource SummaryLabel}"/>
                        <TextBlock x:Name="txtGMT" Text="N/A" Style="{StaticResource SummaryValue}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Displacement" Style="{StaticResource SummaryLabel}"/>
                        <TextBlock x:Name="txtDisplacement" Text="N/A" Style="{StaticResource SummaryValue}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Trim" Style="{StaticResource SummaryLabel}"/>
                        <TextBlock x:Name="txtTrim" Text="N/A" Style="{StaticResource SummaryValue}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="List" Style="{StaticResource SummaryLabel}"/>
                        <TextBlock x:Name="txtList" Text="N/A" Style="{StaticResource SummaryValue}" Margin="0,4,0,0"/>
                    </StackPanel>
                </UniformGrid>
            </Border>

            <!-- ACTION BUTTONS -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                <Button x:Name="btnClear" Content="Clear" Style="{StaticResource BtnClear}" Click="btnClear_Click"/>
            </StackPanel>

            <!-- STABILITY SUMMARY -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Style="{StaticResource AccentBar}" Background="#3B82F6"/>
                        <TextBlock Text="Stability Summary" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <DataGrid Name="dgStabilitySummary" Height="60" 
                              CanUserSortColumns="False" CanUserResizeRows="False" 
                              VerticalScrollBarVisibility="Disabled" IsReadOnly="True"
                              HorizontalScrollBarVisibility="Disabled">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="GMT" Width="*" Binding="{Binding GMT,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}" ElementStyle="{StaticResource CenterCellStyle}"/>
                            <DataGridTextColumn Header="Trim" Width="*" Binding="{Binding TRIM,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}" ElementStyle="{StaticResource CenterCellStyle}"/>
                            <DataGridTextColumn Header="List" Width="*" Binding="{Binding Heel,StringFormat=n2}" HeaderStyle="{StaticResource DGHeader}" ElementStyle="{StaticResource CenterCellStyle}"/>
                            <DataGridTextColumn Header="KG" Width="*" Binding="{Binding [KG(Solid)],StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}" ElementStyle="{StaticResource CenterCellStyle}"/>
                            <DataGridTextColumn Header="KGF" Width="*" Binding="{Binding [KG(Fluid)],StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}" ElementStyle="{StaticResource CenterCellStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- TRIM & LIST + DRAFTS in two columns -->
            <Grid Grid.Row="3" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Trim & List -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Style="{StaticResource AccentBar}" Background="#8B5CF6"/>
                            <TextBlock Text="Trim &amp; List" Style="{StaticResource SectionTitle}"/>
                        </StackPanel>
                        <DataGrid Name="dgTrimList" Height="60" 
                                  CanUserSortColumns="False" CanUserResizeRows="False" 
                                  VerticalScrollBarVisibility="Disabled" IsReadOnly="True"
                                  HorizontalScrollBarVisibility="Disabled">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Trim (m)" Width="*" Binding="{Binding TRIM,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}" ElementStyle="{StaticResource CenterCellStyle}"/>
                                <DataGridTextColumn Header="List (deg)" Width="*" Binding="{Binding Heel,StringFormat=n2}" HeaderStyle="{StaticResource DGHeader}" ElementStyle="{StaticResource CenterCellStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Drafts -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Style="{StaticResource AccentBar}" Background="#EC4899"/>
                            <TextBlock Text="Draft Marks (m)" Style="{StaticResource SectionTitle}"/>
                        </StackPanel>
                        <UniformGrid Columns="5" Margin="0,10,0,0">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="AP" Style="{StaticResource SummaryLabel}" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtDraftAP" Text="--" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Aft" Style="{StaticResource SummaryLabel}" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtDraftAft" Text="--" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Mid" Style="{StaticResource SummaryLabel}" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtDraftMid" Text="--" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Fwd" Style="{StaticResource SummaryLabel}" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtDraftFwd" Text="--" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="FP" Style="{StaticResource SummaryLabel}" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtDraftFP" Text="--" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                        </UniformGrid>
                        <DataGrid Name="dgDraft"
                                  Visibility="Collapsed"
                                  AutoGenerateColumns="True"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserResizeRows="False"/>
                        <DataGrid Name="dgMouldedDraft"
                                  Visibility="Collapsed"
                                  AutoGenerateColumns="True"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserResizeRows="False"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- HYDROSTATICS -->
            <Border Grid.Row="4" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Style="{StaticResource AccentBar}" Background="#14B8A6"/>
                        <TextBlock Text="Hydrostatics" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <DataGrid Name="dgHydrostatics" Height="60" 
                              CanUserSortColumns="False" CanUserResizeRows="False" 
                              VerticalScrollBarVisibility="Disabled" IsReadOnly="True"
                              HorizontalScrollBarVisibility="Auto" ColumnWidth="*">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Displacement (T)" Width="*" Binding="{Binding Displacement,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="LCB (m)" Width="*" Binding="{Binding LCB,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="TCB (m)" Width="*" Binding="{Binding TCB,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="VCB (m)" Width="*" Binding="{Binding VCB,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="LCF (m)" Width="*" Binding="{Binding LCF,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="TCF (m)" Width="*" Binding="{Binding TCF,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="LCG (m)" Width="*" Binding="{Binding LCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="TCG (m)" Width="*" Binding="{Binding TCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="KGF (m)" Width="*" Binding="{Binding KG(Fluid),StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="KMT (m)" Width="*" Binding="{Binding KMT,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="KML (m)" Width="*" Binding="{Binding KML,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="BMT (m)" Width="*" Binding="{Binding BMT,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="BML (m)" Width="*" Binding="{Binding BML,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="WPA (m2)" Width="*" Binding="{Binding WPA,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Buoyancy (T)" Width="*" Binding="{Binding Buoyancy,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- GZ CURVE - FULL WIDTH -->
            <Border Grid.Row="5" Style="{StaticResource CardStyle}" Margin="0,0,0,12" Padding="8">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Style="{StaticResource AccentBar}" Background="#F59E0B"/>
                        <TextBlock Text="GZ Curve" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <Border Background="#F8FAFC" BorderBrush="{StaticResource BorderLight}" BorderThickness="1" CornerRadius="8" Padding="6">
                        <DVC:Chart x:Name="GzChart" Style="{StaticResource ChartStyle1}" Background="#F8FAFC" Padding="5" Foreground="{StaticResource TextPrimary}">
                            <DVC:Chart.Series>
                                <DVC:LineSeries x:Name="GZSeries" Title="GZ" IndependentValueBinding="{Binding Path=Key}" DependentValueBinding="{Binding Path=Value}">
                                    <DVC:LineSeries.DataPointStyle>
                                        <Style TargetType="DVC:LineDataPoint">
                                            <Setter Property="Background" Value="#EF4444"/>
                                            <Setter Property="IsTabStop" Value="False"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="DVC:LineDataPoint">
                                                        <Canvas/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </DVC:LineSeries.DataPointStyle>
                                </DVC:LineSeries>
                                <DVC:LineSeries x:Name="WHSeries" Title="Wind Heeling" IndependentValueBinding="{Binding Path=Key}" DependentValueBinding="{Binding Path=Value}">
                                    <DVC:LineSeries.DataPointStyle>
                                        <Style TargetType="DVC:LineDataPoint">
                                            <Setter Property="Background" Value="#3B82F6"/>
                                            <Setter Property="IsTabStop" Value="False"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="DVC:LineDataPoint">
                                                        <Canvas/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </DVC:LineSeries.DataPointStyle>
                            </DVC:LineSeries>
                            <DVC:LineSeries x:Name="HLSeries" Title="Heavy Weights" IndependentValueBinding="{Binding Path=Key}" DependentValueBinding="{Binding Path=Value}"/>
                            <DVC:LineSeries x:Name="HSSeries" Title="Turning" IndependentValueBinding="{Binding Path=Key}" DependentValueBinding="{Binding Path=Value}"/>
                            <DVC:LineSeries x:Name="PCSeries" Title="Crowding" IndependentValueBinding="{Binding Path=Key}" DependentValueBinding="{Binding Path=Value}"/>
                        </DVC:Chart.Series>
                        <DVC:Chart.Axes>
                            <DVC:LinearAxis Orientation="Y" Title="GZ (m)"/>
                            <DVC:LinearAxis Orientation="X" Title="Heel (deg)"/>
                        </DVC:Chart.Axes>
                        </DVC:Chart>
                    </Border>
                </StackPanel>
            </Border>

            <!-- GZ AND HEELING ARMS -->
            <Border x:Name="groupBox2" Grid.Row="6" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Style="{StaticResource AccentBar}" Background="#6366F1"/>
                        <TextBlock Text="GZ and Heeling Arms" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <DataGrid Name="dgGZ" 
                              CanUserSortColumns="False" CanUserResizeRows="False" 
                              IsReadOnly="True" HorizontalScrollBarVisibility="Auto" ColumnWidth="*">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Heel (deg)" Width="*" Binding="{Binding HeelAngle,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="GZ (m)" Width="*" Binding="{Binding GZ,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Wind Heeling (m)" Width="*" Binding="{Binding WH,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Heavy Weights (m)" Width="*" Binding="{Binding HL,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Turning (m)" Width="*" Binding="{Binding HS,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Crowding (m)" Width="*" Binding="{Binding PC,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- STABILITY CRITERIA -->
            <Border Grid.Row="7" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Style="{StaticResource AccentBar}" Background="#10B981"/>
                        <TextBlock x:Name="txtStabilityCriteriaTitle" Text="Stability Criteria" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <DataGrid Name="dgStability" 
                              CanUserSortColumns="False" CanUserResizeRows="False" 
                              IsReadOnly="True" HorizontalScrollBarVisibility="Disabled" ColumnWidth="*">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Criterion" Width="2*" Binding="{Binding Criterion}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Critical Value" Width="*" Binding="{Binding Critical_Value,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Actual Value" Width="*" Binding="{Binding Actual_Value,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTemplateColumn Header="Status" Width="*" HeaderStyle="{StaticResource DGHeader}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="6" Padding="10,4" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#FEE2E2"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Pass">
                                                            <Setter Property="Background" Value="#DCFCE7"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock HorizontalAlignment="Center" FontWeight="Bold" FontSize="12">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#DC2626"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Pass">
                                                                <Setter Property="Foreground" Value="#16A34A"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                                <Run Text="{Binding Status, Mode=OneWay}"/>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- LOADING SUMMARY -->
            <Border Grid.Row="8" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Style="{StaticResource AccentBar}" Background="#0EA5E9"/>
                        <TextBlock Text="Loading Summary" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <DataGrid Name="dgLoadingSummary" 
                              CanUserSortColumns="False" CanUserResizeRows="False" 
                              IsReadOnly="True" HorizontalScrollBarVisibility="Auto"
                              MaxHeight="280" SelectionChanged="dgLoadingSummary_SelectionChanged" ColumnWidth="*">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tank Name" Width="1.5*" Binding="{Binding Tank_Name}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="% Full" Width="*" Binding="{Binding Percent_Full,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Volume (m3)" Width="*" Binding="{Binding Volume,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="SG" Width="0.7*" Binding="{Binding SG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Weight (T)" Width="*" Binding="{Binding Weight,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="LCG (m)" Width="*" Binding="{Binding LCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="TCG (m)" Width="*" Binding="{Binding TCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="VCG (m)" Width="*" Binding="{Binding VCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="FSM (T-m)" Width="*" Binding="{Binding FSM,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- DAMAGED TANKS -->
            <Border x:Name="gbSoundingPer" Grid.Row="9" Style="{StaticResource CardStyle}" Margin="0,0,0,2">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Style="{StaticResource AccentBar}" Background="#F97316"/>
                        <TextBlock Text="Damaged Tanks" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <DataGrid Name="dgSoundingPer" 
                              CanUserSortColumns="False" CanUserResizeRows="False" 
                              IsReadOnly="True" HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Width="200" Binding="{Binding Tank_Name}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="% Full" Width="100" Binding="{Binding Percent_Full,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Volume (m3)" Width="120" Binding="{Binding CapacityMC,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="Weight (T)" Width="110" Binding="{Binding CapacityTonnes,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="LCG (m)" Width="100" Binding="{Binding LCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="TCG (m)" Width="100" Binding="{Binding TCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                            <DataGridTextColumn Header="VCG (m)" Width="100" Binding="{Binding VCG,StringFormat=n3}" HeaderStyle="{StaticResource DGHeader}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
