<Page x:Class="WpfMvvmStability.Views.RealModeMain"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
	  xmlns:viewModels="clr-namespace:WpfMvvmStability.ViewModels"
      xmlns:DBData="clr-namespace:WpfMvvmStability"
      xmlns:extbehaviour="clr-namespace:WPFExtensions.AttachedBehaviours;assembly=WPFExtensions"
      xmlns:cmd="clr-namespace:WPFExtensions.ViewModel.Commanding;assembly=WPFExtensions"
      xmlns:local="clr-namespace:WpfMvvmStability"
      xmlns:System="clr-namespace:System;assembly=mscorlib"
      xmlns:Controls="clr-namespace:WPFExtensions.Controls;assembly=WPFExtensions"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="RealModeMain" Background="{DynamicResource PageBgBrush}" FontFamily="Segoe UI" Loaded="Page_Loaded">

	<Page.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/Stability;component/Views/TankSectionStyles.xaml"/>
			</ResourceDictionary.MergedDictionaries>

			<!-- Modern Colors -->
			<Color x:Key="PrimaryColor">#1F48FF</Color>
			<SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
			<SolidColorBrush x:Key="PrimaryBgBrush" Color="#EEF1FF"/>
			<SolidColorBrush x:Key="PrimaryBorderBrush" Color="#B3C0FF"/>
			<SolidColorBrush x:Key="TextPrimaryBrush" Color="#030032"/>
			<SolidColorBrush x:Key="TextSecondaryBrush" Color="#4A4A6A"/>
			<SolidColorBrush x:Key="CardBgBrush" Color="#FFFFFF"/>
			<SolidColorBrush x:Key="InnerBgBrush" Color="#F1F5FB"/>
			<SolidColorBrush x:Key="BorderBrush" Color="#E2E5F0"/>
			<SolidColorBrush x:Key="DividerBrush" Color="#D0D4E4"/>

			<!-- Tank Section Frames -->
			<Style x:Key="SectionFrameBlue" TargetType="Border">
				<Setter Property="BorderBrush" Value="#3B82F6"/>
				<Setter Property="BorderThickness" Value="1"/>
				<Setter Property="CornerRadius" Value="8"/>
				<Setter Property="Margin" Value="0,0,0,10"/>
			</Style>
			<Style x:Key="SectionFrameGreen" TargetType="Border">
				<Setter Property="BorderBrush" Value="#10B981"/>
				<Setter Property="BorderThickness" Value="1"/>
				<Setter Property="CornerRadius" Value="8"/>
				<Setter Property="Margin" Value="0,0,0,10"/>
			</Style>
			<Style x:Key="SectionFrameOrange" TargetType="Border">
				<Setter Property="BorderBrush" Value="#F59E0B"/>
				<Setter Property="BorderThickness" Value="1"/>
				<Setter Property="CornerRadius" Value="8"/>
				<Setter Property="Margin" Value="0,0,0,10"/>
			</Style>

			<Style x:Key="TankPanelTabItemStyle" TargetType="TabItem">
				<Setter Property="Background" Value="#F8F9FC"/>
				<Setter Property="Foreground" Value="#4A4A6A"/>
				<Setter Property="Padding" Value="14,8"/>
				<Setter Property="Margin" Value="0,0,4,0"/>
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="TabItem">
							<Border Name="Border" Background="{TemplateBinding Background}" CornerRadius="6,6,0,0" Padding="{TemplateBinding Padding}">
								<ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="IsSelected" Value="True">
									<Setter TargetName="Border" Property="Background" Value="#FFFFFF"/>
									<Setter Property="Foreground" Value="#1F48FF"/>
									<Setter Property="FontWeight" Value="SemiBold"/>
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>

		<!-- Expander Style -->
		<Style x:Key="TankSectionExpander" TargetType="Expander">
			<Setter Property="Margin" Value="0,0,0,10"/>
			<Setter Property="Background" Value="{DynamicResource CardBgBrush}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
			<Setter Property="BorderThickness" Value="1.2"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
			<Setter Property="FontFamily" Value="Segoe UI"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Padding" Value="0"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Expander">
						<Border x:Name="OuterBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto"/>
									<RowDefinition Height="Auto"/>
								</Grid.RowDefinitions>

								<ToggleButton x:Name="HeaderSite"
                                              Grid.Row="0"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              Cursor="Hand"
                                              FocusVisualStyle="{x:Null}"
                                              IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Padding="12,10"
                                              HorizontalContentAlignment="Stretch"
                                              VerticalContentAlignment="Center"
                                              Foreground="{TemplateBinding Foreground}"
                                              FontFamily="{TemplateBinding FontFamily}"
                                              FontSize="{TemplateBinding FontSize}"
                                              FontWeight="{TemplateBinding FontWeight}"
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}">
									<ToggleButton.Template>
										<ControlTemplate TargetType="ToggleButton">
											<Border x:Name="HeaderBorder"
                                                    Background="Transparent"
                                                    CornerRadius="10"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="1">
												<Grid>
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="4"/>
														<ColumnDefinition Width="34"/>
														<ColumnDefinition Width="*"/>
														<ColumnDefinition Width="Auto"/>
													</Grid.ColumnDefinitions>

													<Border Grid.Column="0"
                                                            Background="{TemplateBinding BorderBrush}"
                                                            CornerRadius="9,0,0,9"/>

													<Border Grid.Column="1"
                                                            Width="26"
                                                            Height="28"
                                                            Background="{DynamicResource PageBgBrush}"
                                                            BorderBrush="{DynamicResource BorderBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="7"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Center">
														<Grid Margin="4">
															<Grid.RowDefinitions>
																<RowDefinition Height="*"/>
																<RowDefinition Height="*"/>
																<RowDefinition Height="*"/>
															</Grid.RowDefinitions>
															<Rectangle Grid.Row="0" Height="2" Fill="{TemplateBinding BorderBrush}" RadiusX="1" RadiusY="1"/>
															<Rectangle Grid.Row="1" Height="2" Fill="{TemplateBinding BorderBrush}" RadiusX="1" RadiusY="1"/>
															<Rectangle Grid.Row="2" Height="2" Fill="{TemplateBinding BorderBrush}" RadiusX="1" RadiusY="1"/>
														</Grid>
													</Border>

													<ContentPresenter Grid.Column="2"
                                                                      Margin="8,0,10,0"
                                                                      VerticalAlignment="Center"
                                                                      RecognizesAccessKey="True"
                                                                      Content="{TemplateBinding Content}"
                                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                      ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"/>

													<Grid Grid.Column="3"
                                                          Width="28"
                                                          Height="28"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center">
														<Path x:Name="Chevron"
                                                              Width="10"
                                                              Height="6"
                                                              Stretch="Fill"
                                                              Fill="{TemplateBinding Foreground}"
                                                              Data="M 0 0 L 5 5 L 10 0 Z"
                                                              RenderTransformOrigin="0.5,0.5">
															<Path.RenderTransform>
																<RotateTransform x:Name="ChevronTransform" Angle="0"/>
															</Path.RenderTransform>
														</Path>
													</Grid>
												</Grid>
											</Border>
											<ControlTemplate.Triggers>
												<Trigger Property="IsMouseOver" Value="True">
													<Setter TargetName="HeaderBorder" Property="Background" Value="#18224F"/>
												</Trigger>
												<Trigger Property="IsChecked" Value="True">
													<Setter TargetName="HeaderBorder" Property="Background" Value="#1D285A"/>
												</Trigger>
												<Trigger Property="IsEnabled" Value="False">
													<Setter TargetName="HeaderBorder" Property="Opacity" Value="0.55"/>
												</Trigger>
											</ControlTemplate.Triggers>
										</ControlTemplate>
									</ToggleButton.Template>
								</ToggleButton>

								<ContentPresenter x:Name="ExpandSite"
                                                  Grid.Row="1"
                                                  Margin="12,0,12,12"
                                                  Visibility="Collapsed"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"/>
							</Grid>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsExpanded" Value="True">
								<Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- Value Labels -->
		<Style TargetType="Label" x:Key="ValueLabel">
			<Setter Property="Background" Value="{DynamicResource PrimaryBgBrush}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource PrimaryBorderBrush}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="8,3"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="MinWidth" Value="118"/>
			<Setter Property="MinHeight" Value="28"/>
			<Setter Property="FontFamily" Value="Segoe UI"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="BorderBrush" Value="#3B82F6"/>
					<Setter Property="Background" Value="{DynamicResource PrimaryBgBrush}"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<Style TargetType="Label" x:Key="HeaderLabel">
			<Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Padding" Value="0"/>
			<Setter Property="Margin" Value="0,0,3,0"/>
			<Setter Property="FontFamily" Value="Segoe UI"/>
		</Style>
		</ResourceDictionary>
	</Page.Resources>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" MinWidth="720" />
			<ColumnDefinition Width="360" MinWidth="320" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="250" />
		</Grid.RowDefinitions>

		<Image x:Name="imgThemeWatermark"
               Grid.ColumnSpan="2"
               Grid.RowSpan="3"
               Panel.ZIndex="99"
               IsHitTestVisible="False"
               Stretch="Uniform"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Margin="70"
               Source="pack://application:,,,/Assets/Images/watermark_light.png"
               Opacity="0.08"
               RenderOptions.BitmapScalingMode="HighQuality"/>

		<!-- ================= TOP: GRAPHICS ================= -->
		<Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Margin="4,4,6,6" Background="{DynamicResource CardBgBrush}" CornerRadius="8" BorderBrush="#3B82F6" BorderThickness="1.5">
			<Border.Effect>
				<DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.03" Color="#030032"/>
			</Border.Effect>
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>

					<TabControl Grid.Row="0" Name="tabControl2" IsEnabled="True" Background="Transparent" BorderThickness="0" SelectionChanged="tabControl2_SelectionChanged">
					<TabControl.Resources>
						<SolidColorBrush x:Key="AppBackground" Color="#FFFFFF"/>
						<SolidColorBrush x:Key="ContentBackground" Color="#FFFFFF"/>
						<SolidColorBrush x:Key="AccentBrush" Color="#1F48FF"/>
						<SolidColorBrush x:Key="TextPrimary" Color="#030032"/>
						<SolidColorBrush x:Key="TextSecondary" Color="#4A4A6A"/>
						<SolidColorBrush x:Key="CanvasPaper" Color="#FFFFFF"/>

						<Style x:Key="MainTabItemStyle" TargetType="TabItem">
							<Setter Property="Background" Value="#F2F5FA"/>
							<Setter Property="Foreground" Value="#030032"/>
							<Setter Property="FontSize" Value="13"/>
							<Setter Property="FontWeight" Value="SemiBold"/>
							<Setter Property="Padding" Value="20,10"/>
							<Setter Property="Margin" Value="2,0,4,0"/>
							<Setter Property="Template">
								<Setter.Value>
									<ControlTemplate TargetType="TabItem">
										<Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="#C9D4E6" BorderThickness="1" CornerRadius="8,8,0,0" Padding="{TemplateBinding Padding}">
											<ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
										</Border>
										<ControlTemplate.Triggers>
											<Trigger Property="IsSelected" Value="True">
												<Setter TargetName="Border" Property="Background" Value="#1F48FF"/>
												<Setter TargetName="Border" Property="BorderBrush" Value="#0B1F4D"/>
												<Setter Property="Foreground" Value="White"/>
												<Setter Property="FontWeight" Value="Bold"/>
											</Trigger>
											<Trigger Property="IsMouseOver" Value="True">
												<Setter TargetName="Border" Property="BorderBrush" Value="#1F48FF"/>
											</Trigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</Setter.Value>
							</Setter>
						</Style>

						<Style x:Key="VerticalTabItemStyle" TargetType="TabItem">
							<Setter Property="Background" Value="#F3F6FC"/>
							<Setter Property="BorderBrush" Value="#D8E1EF"/>
							<Setter Property="BorderThickness" Value="1"/>
							<Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
							<Setter Property="FontSize" Value="12"/>
							<Setter Property="Padding" Value="10,8"/>
							<Setter Property="Margin" Value="4,3"/>
							<Setter Property="Template">
								<Setter.Value>
									<ControlTemplate TargetType="TabItem">
										<Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
											<ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header">
												<ContentPresenter.LayoutTransform>
													<RotateTransform Angle="90"/>
												</ContentPresenter.LayoutTransform>
											</ContentPresenter>
										</Border>
										<ControlTemplate.Triggers>
											<Trigger Property="IsSelected" Value="True">
												<Setter TargetName="Border" Property="Background" Value="#1F48FF"/>
												<Setter Property="Foreground" Value="White"/>
												<Setter Property="FontWeight" Value="Bold"/>
											</Trigger>
											<Trigger Property="IsMouseOver" Value="True">
												<Setter TargetName="Border" Property="Background" Value="#EAF0FF"/>
											</Trigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</Setter.Value>
							</Setter>
						</Style>

						<Style TargetType="Button" x:Key="ModernButtonStyle">
							<Setter Property="Background" Value="#1F48FF"/>
							<Setter Property="Foreground" Value="White"/>
							<Setter Property="BorderThickness" Value="0"/>
							<Setter Property="Padding" Value="15,6"/>
							<Setter Property="FontSize" Value="12"/>
							<Setter Property="Cursor" Value="Hand"/>
							<Setter Property="Template">
								<Setter.Value>
									<ControlTemplate TargetType="Button">
										<Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
											<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
										</Border>
										<ControlTemplate.Triggers>
											<Trigger Property="IsMouseOver" Value="True">
												<Setter Property="Background" Value="#4A6FFF"/>
											</Trigger>
											<Trigger Property="IsPressed" Value="True">
												<Setter Property="Background" Value="#1636CC"/>
											</Trigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</Setter.Value>
							</Setter>
						</Style>
					</TabControl.Resources>

					<!-- Graphics 2D -->
					<TabItem Header="Graphics 2D" Name="tabItem3" Style="{StaticResource MainTabItemStyle}">
						<Grid Background="{DynamicResource CardBgBrush}">
							<Border BorderBrush="{DynamicResource GraphicsFrameBrush}"
							        BorderThickness="2"
							        CornerRadius="10"
							        Margin="4,4,6,6"
							        Background="{DynamicResource ShellSurfaceBrush}"
							        Padding="2">
					<TabControl Name="tabControl5" TabStripPlacement="Left" Background="Transparent" BorderThickness="0" SelectionChanged="tabControl5_SelectionChanged">
								<TabControl.Resources>
									<Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
										<Setter Property="Padding" Value="12,10"/>
										<Setter Property="Template">
											<Setter.Value>
												<ControlTemplate TargetType="TabItem">
													<Border Name="Border" Background="{DynamicResource PageBgBrush}" BorderBrush="{DynamicResource GraphicsRailBrush}" BorderThickness="1.2" Margin="4,3" Padding="7" CornerRadius="8" SnapsToDevicePixels="True">
														<ContentPresenter ContentSource="Header">
															<ContentPresenter.LayoutTransform>
																<RotateTransform Angle="90"/>
															</ContentPresenter.LayoutTransform>
														</ContentPresenter>
													</Border>
													<ControlTemplate.Triggers>
														<Trigger Property="IsSelected" Value="True">
															<Setter TargetName="Border" Property="Background" Value="#1F48FF"/>
															<Setter TargetName="Border" Property="BorderBrush" Value="#0B1F4D"/>
															<Setter Property="Foreground" Value="White"/>
															<Setter Property="FontWeight" Value="Bold"/>
														</Trigger>
														<MultiTrigger>
															<MultiTrigger.Conditions>
																<Condition Property="IsMouseOver" Value="True"/>
																<Condition Property="IsSelected" Value="False"/>
															</MultiTrigger.Conditions>
															<Setter TargetName="Border" Property="Background" Value="#EAF0FF"/>
														</MultiTrigger>
													</ControlTemplate.Triggers>
												</ControlTemplate>
											</Setter.Value>
										</Setter>
									</Style>
								</TabControl.Resources>

								<TabItem Height="100" Name="tabItem7">
									<TabItem.Header>
										<TextBlock Text="PROFILE" HorizontalAlignment="Center" />
									</TabItem.Header>
									<Grid Background="{DynamicResource PageBgBrush}">
										<DockPanel Name="dockPanelProfile" LastChildFill="True">
											<Border Background="{DynamicResource InnerBgBrush}" BorderBrush="{DynamicResource GraphicsInnerFrameBrush}" BorderThickness="1.5" CornerRadius="8" Margin="5,6,5,10" ClipToBounds="True">
												<Grid>
													<Canvas Background="Transparent"
															Name="canvas2DProfile"
															Cursor="SizeAll"
															PreviewMouseWheel="Canvas2D_MouseWheel"
															PreviewMouseLeftButtonDown="Canvas2D_MouseLeftButtonDown"
															PreviewMouseLeftButtonUp="Canvas2D_MouseLeftButtonUp"
															PreviewMouseMove="Canvas2D_MouseMove"
															MouseWheel="Canvas2D_MouseWheel"
															MouseLeftButtonDown="Canvas2D_MouseLeftButtonDown"
															MouseLeftButtonUp="Canvas2D_MouseLeftButtonUp"
															MouseMove="Canvas2D_MouseMove"/>
												</Grid>
											</Border>
										</DockPanel>
									</Grid>
								</TabItem>
								<TabItem Height="100" Name="tabItem8">
									<TabItem.Header>
										<TextBlock Text="MAIN DECK" HorizontalAlignment="Center" />
									</TabItem.Header>
									<Grid Background="{DynamicResource PageBgBrush}">
										<DockPanel Name="dockPanelBottom" LastChildFill="True">
											<Border Background="{DynamicResource InnerBgBrush}" BorderBrush="{DynamicResource GraphicsInnerFrameBrush}" BorderThickness="1.5" CornerRadius="8" Margin="5,6,5,10" ClipToBounds="True">
												<Grid>
													<Canvas Background="Transparent"
															Name="canvas2DPlanA"
															Cursor="SizeAll"
															PreviewMouseWheel="Canvas2D_MouseWheel"
															PreviewMouseLeftButtonDown="Canvas2D_MouseLeftButtonDown"
															PreviewMouseLeftButtonUp="Canvas2D_MouseLeftButtonUp"
															PreviewMouseMove="Canvas2D_MouseMove"
															MouseWheel="Canvas2D_MouseWheel"
															MouseLeftButtonDown="Canvas2D_MouseLeftButtonDown"
															MouseLeftButtonUp="Canvas2D_MouseLeftButtonUp"
															MouseMove="Canvas2D_MouseMove"/>
												</Grid>
											</Border>
										</DockPanel>
										<Controls:ZoomControl x:Name="zoomControlBottom" Visibility="Collapsed"/>
									</Grid>
								</TabItem>
								<TabItem Height="100" Name="tabItem9">
									<TabItem.Header>
										<TextBlock Text="TANK TOP" HorizontalAlignment="Center" />
									</TabItem.Header>
									<Grid Background="{DynamicResource PageBgBrush}">
										<DockPanel Name="dockPanelDeck" LastChildFill="True">
											<Border Background="{DynamicResource InnerBgBrush}" BorderBrush="{DynamicResource GraphicsInnerFrameBrush}" BorderThickness="1.5" CornerRadius="8" Margin="5,6,5,10" ClipToBounds="True">
												<Grid>
													<Canvas Background="Transparent"
															Name="canvas2DPlanB"
															Cursor="SizeAll"
															PreviewMouseWheel="Canvas2D_MouseWheel"
															PreviewMouseLeftButtonDown="Canvas2D_MouseLeftButtonDown"
															PreviewMouseLeftButtonUp="Canvas2D_MouseLeftButtonUp"
															PreviewMouseMove="Canvas2D_MouseMove"
															MouseWheel="Canvas2D_MouseWheel"
															MouseLeftButtonDown="Canvas2D_MouseLeftButtonDown"
															MouseLeftButtonUp="Canvas2D_MouseLeftButtonUp"
															MouseMove="Canvas2D_MouseMove"/>
												</Grid>
											</Border>
										</DockPanel>
										<Controls:ZoomControl x:Name="zoomControlDeck" Visibility="Collapsed"/>
									</Grid>
								</TabItem>
								</TabControl>
							</Border>
							<Grid Visibility="Collapsed">
								<DockPanel Background="{DynamicResource CardBgBrush}" Name="dockPanel1">
									<Controls:ZoomControl Background="{DynamicResource CardBgBrush}" Foreground="{x:Null}" Mode="Fill" ModifierMode="ZoomIn" Name="zoomControl1" ZoomBoxBackground="{x:Null}" Width="1025" MinZoom="3">
										<Controls:ZoomControl.Resources>
											<Style TargetType="{x:Type Rectangle}">
												<Setter Property="extbehaviour:DragBehaviour.IsDragEnabled" Value="True"/>
												<Setter Property="extbehaviour:DragBehaviour.X" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Canvas.Left)}"/>
												<Setter Property="extbehaviour:DragBehaviour.Y" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Canvas.Top)}"/>
											</Style>
										</Controls:ZoomControl.Resources>
										<Grid Background="{DynamicResource CardBgBrush}" Height="52.5" Width="180">
											
											<Canvas Background="{DynamicResource CardBgBrush}" Name="canvas2DPlanC" Margin="0,0,-0.031,0"/>
										</Grid>
									</Controls:ZoomControl>
								</DockPanel>
								<DockPanel Background="{DynamicResource CardBgBrush}" Name="dockPanel21">
									<Controls:ZoomControl Background="{DynamicResource CardBgBrush}" Foreground="{x:Null}" Mode="Fill" ModifierMode="ZoomIn" Name="zoomControl21" ZoomBoxBackground="{x:Null}" Width="1025" RenderTransformOrigin="0,0" MinZoom="3">
										<Controls:ZoomControl.Resources>
											<Style TargetType="{x:Type Rectangle}">
												<Setter Property="extbehaviour:DragBehaviour.IsDragEnabled" Value="True"/>
												<Setter Property="extbehaviour:DragBehaviour.X" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Canvas.Left)}"/>
												<Setter Property="extbehaviour:DragBehaviour.Y" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Canvas.Top)}"/>
											</Style>
										</Controls:ZoomControl.Resources>
										<Grid Background="{DynamicResource CardBgBrush}" Height="52.5" Width="177.5">
											
											<Canvas Background="Transparent" Name="canvas2DPlanALL" Margin="0,0,-0.031,0" RenderTransformOrigin="0,0" Focusable="True" Opacity="1"/>
										</Grid>
									</Controls:ZoomControl>
								</DockPanel>
							</Grid>
							<Border x:Name="overlay2DLoading"
									Visibility="Collapsed"
									Background="#D9FFFFFF"
									CornerRadius="10"
									Margin="16">
								<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="240">
									<TextBlock x:Name="txt2DLoadingTitle"
											   Text="Loading 2D drawings..."
											   FontFamily="Segoe UI"
											   FontSize="16"
											   FontWeight="SemiBold"
											   Foreground="{DynamicResource TextPrimaryBrush}"
											   HorizontalAlignment="Center"/>
									<TextBlock x:Name="txt2DLoadingHint"
											   Text="Preparing profile and deck plans"
											   FontFamily="Segoe UI"
											   FontSize="12"
											   Margin="0,6,0,12"
											   Foreground="{DynamicResource TextSecondaryBrush}"
											   HorizontalAlignment="Center"/>
									<ProgressBar IsIndeterminate="True"
												 Height="8"
												 Foreground="#1F48FF"
												 Background="#E5EAFF"/>
								</StackPanel>
							</Border>
						</Grid>
					</TabItem>

					<!-- Graphics 3D -->
					<TabItem Header="Graphics 3D" Name="tabItem6" Style="{StaticResource MainTabItemStyle}">
						<Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Name="border2"
									Margin="12" CornerRadius="12" Background="{DynamicResource CardBgBrush}">

							<Grid>
								
								<hx:Viewport3DX x:Name="viewPort3d1"
		ShowCoordinateSystem="False"
		ShowViewCube="True"
		ViewCubeSize="88"
		ViewCubeHorizontalPosition="12"
		ViewCubeVerticalPosition="12"
		IsViewCubeEdgeClicksEnabled="True"
		IsViewCubeMoverEnabled="True"
		IsCoordinateSystemMoverEnabled="False"
		ShowCameraTarget="False"
		CameraMode="Inspect"
		ZoomAroundMouseDownPoint="False"
		RotateAroundMouseDownPoint="False"
		IsPanEnabled="True"
		CameraRotationMode="Trackball"
		Loaded="viewPort3d_Loaded"
		SizeChanged="ViewPort3d1_SizeChanged"
		BackgroundColor="Transparent">
									<hx:Viewport3DX.Camera>
										<hx:PerspectiveCamera Position="80000,60000,40000"
				LookDirection="-80000,-60000,-40000"
				UpDirection="0,0,1"
				FieldOfView="45"
				NearPlaneDistance="0.5"
				FarPlaneDistance="500000"/>
									</hx:Viewport3DX.Camera>
									<hx:DirectionalLight3D Direction="-1,-1,-1" Color="White"/>
									<hx:DirectionalLight3D Direction="1,1,1" Color="#999999"/>
									<hx:DirectionalLight3D Direction="0,0,1" Color="#666666"/>
									<hx:AmbientLight3D Color="#444444"/>
									<hx:GroupModel3D x:Name="scene3D"/>
								</hx:Viewport3DX>

								<StackPanel Margin="14" VerticalAlignment="Bottom" HorizontalAlignment="Left">
									<TextBlock Text="{Binding Tankname, StringFormat='Tank Name: {0}'}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
									<TextBlock Text="{Binding volume, StringFormat='Volume: {0}'}" Foreground="{DynamicResource TextSecondaryBrush}"/>
									<TextBlock Text="{Binding percent, StringFormat='Percent Fill: {0} %'}" Foreground="{DynamicResource TextSecondaryBrush}"/>
									<TextBlock Text="{Binding status, StringFormat='Status: {0}'}" Foreground="{DynamicResource TextSecondaryBrush}"/>
								</StackPanel>

								<StackPanel Margin="14" VerticalAlignment="Top" HorizontalAlignment="Right">
									<Button x:Name="btn3DZoomExtents" 
											Content="RESET VIEW" 
											Style="{StaticResource ModernButtonStyle}"
											Click="btn3DZoomExtents_Click"
											Width="118"
											Height="34"
											Padding="10,4"
											Margin="0,0,0,8">
										<Button.Template>
											<ControlTemplate TargetType="Button">
												<Border Background="{TemplateBinding Background}"
														BorderBrush="{TemplateBinding BorderBrush}"
														BorderThickness="{TemplateBinding BorderThickness}"
														CornerRadius="4">
													<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
												</Border>
											</ControlTemplate>
										</Button.Template>
										<Button.Effect>
											<DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.2"/>
										</Button.Effect>
									</Button>
								</StackPanel>

								<Border x:Name="overlay3DLoading"
										Visibility="Collapsed"
										Background="#D9FFFFFF"
										CornerRadius="10"
										Margin="12">
									<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="240">
										<TextBlock x:Name="txt3DLoadingTitle"
												   Text="Loading 3D model..."
												   FontFamily="Segoe UI"
												   FontSize="16"
												   FontWeight="SemiBold"
												   Foreground="{DynamicResource TextPrimaryBrush}"
												   HorizontalAlignment="Center"/>
										<TextBlock x:Name="txt3DLoadingHint"
												   Text="Please wait a few seconds. The model will appear shortly."
												   FontFamily="Segoe UI"
												   FontSize="12"
												   Margin="0,6,0,12"
												   Foreground="{DynamicResource TextSecondaryBrush}"
												   HorizontalAlignment="Center"/>
										<ProgressBar IsIndeterminate="True"
													 Height="8"
													 Foreground="#1F48FF"
													 Background="#E5EAFF"/>
									</StackPanel>
								</Border>
							</Grid>
						</Border>
					</TabItem>
				</TabControl>
			</Grid>
		</Border>

		<Border Grid.Row="0" Grid.Column="1" Margin="6,4,5,6">
			<Border Background="{DynamicResource CardBgBrush}" BorderBrush="#3B82F6" BorderThickness="1.5" CornerRadius="10" Padding="12">
				<Border.Effect>
					<DropShadowEffect ShadowDepth="0" BlurRadius="4" Opacity="0.04" Color="#030032"/>
				</Border.Effect>
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>
					<StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="2,0,12,0">
                        <TextBlock Text="Calculation Progress" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
							<Ellipse Name="progressStatusDot" Width="8" Height="8" Fill="#10B981" VerticalAlignment="Center" Margin="0,0,6,0"/>
							<TextBlock Name="txtProgressStatus" Text="" FontSize="12" FontWeight="SemiBold" Foreground="#10B981" VerticalAlignment="Center"/>
						</StackPanel>
					</StackPanel>
					<Border Grid.Row="0" Grid.Column="1"
					        Height="40" MinWidth="132"
					        Padding="14,8"
					        Background="#E9F8EF"
					        BorderBrush="#10B981"
					        BorderThickness="1"
					        CornerRadius="10"
					        HorizontalAlignment="Right"
					        VerticalAlignment="Center">
						<StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
							<Ellipse Width="8" Height="8" Fill="#10B981" Margin="0,0,8,0"/>
							<TextBlock Text="Auto Refresh" FontWeight="Bold" FontSize="12" Foreground="#047857"/>
						</StackPanel>
					</Border>
					<Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Background="#EDF4FF" BorderBrush="#C7DAFF" BorderThickness="1" CornerRadius="10" Padding="10,8" Margin="0,10,0,0">
						<Grid>
							<ProgressBar Name="pRGSCalculation" Height="14" Minimum="0" Maximum="100" Value="0" Background="#D8E4FF" Foreground="#1F48FF"/>
							<TextBlock Name="txtProgressPercent" Text="0%" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="11" Foreground="#1636CC"/>
						</Grid>
					</Border>
				</Grid>
			</Border>
		</Border>

		<!-- ================= RIGHT PANEL: OUTPUTS ================= -->
		<Border Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Margin="6,0,5,5" Background="{DynamicResource CardBgBrush}" CornerRadius="12" BorderBrush="#10B981" BorderThickness="1.5">
			<Border.Effect>
				<DropShadowEffect ShadowDepth="0" BlurRadius="0.5" Opacity="0.01" Color="#030032"/>
			</Border.Effect>
			<TabControl Name="tabControl3" Background="Transparent" BorderThickness="0" SelectedIndex="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource TankPanelTabItemStyle}"/>
                </TabControl.Resources>
                <TabControl.Template>
                    <ControlTemplate TargetType="TabControl">
                        <ContentPresenter ContentSource="SelectedContent"/>
                    </ControlTemplate>
                </TabControl.Template>
				<TabItem x:Name="tabItem4" Visibility="Collapsed">
					<Grid>
						<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
							<StackPanel Margin="8,6,8,8">

								<!-- Action Buttons -->
								<Border Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="6" Margin="0,0,0,6">
									<Border.Effect>
										<DropShadowEffect ShadowDepth="0" BlurRadius="0.5" Opacity="0.008" Color="#030032"/>
									</Border.Effect>
									<Grid HorizontalAlignment="Stretch">
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="10"/>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="10"/>
											<ColumnDefinition Width="*"/>
										</Grid.ColumnDefinitions>
										<Button Grid.Column="0" Content="Import" Name="btnImportRealModeData" Click="btnImportRealModeData_Click" Height="34" HorizontalAlignment="Stretch" Background="#1F48FF" Foreground="White" BorderBrush="#7EA2FF" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
										<Button Grid.Column="2" Content="Save" Name="btnSaveLoadingCondition" Click="btnSaveLoadingCondition_Click" Height="34" HorizontalAlignment="Stretch" Background="#2E5BFF" Foreground="White" BorderBrush="#9DB3FF" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
										<Button Grid.Column="4" Content="Clear" Name="btnclear" Click="btnclearOutputPanel_Click" Height="34" HorizontalAlignment="Stretch" Background="#1636CC" Foreground="White" BorderBrush="#7EA2FF" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
									</Grid>
								</Border>

								<!-- Output Panels Grid -->
								<Grid Margin="0,6,0,0">
									<Grid.RowDefinitions>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
									</Grid.RowDefinitions>

									<!-- Row 1: Stability Summary -->
									<Grid Grid.Row="0">
										<Grid.RowDefinitions>
											<RowDefinition Height="Auto"/>
										</Grid.RowDefinitions>
										<!-- Stability Summary Box -->
										<Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,0,0,6">
											<Border.Effect>
												<DropShadowEffect ShadowDepth="0" BlurRadius="0.5" Opacity="0.008" Color="#030032"/>
											</Border.Effect>
											<StackPanel>
												<Border Background="{DynamicResource PrimaryBrush}" CornerRadius="6" Padding="10,5" Margin="0,0,0,8" HorizontalAlignment="Stretch">
													<TextBlock Text="Stability Summary" FontWeight="Bold" Foreground="White" FontSize="12" Margin="2,0,0,0"/>
												</Border>
												<Grid Margin="2,0,2,0">
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="*"/>
														<ColumnDefinition Width="118"/>
														<ColumnDefinition Width="72"/>
													</Grid.ColumnDefinitions>
													<Grid.RowDefinitions>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
													</Grid.RowDefinitions>
													<Label Grid.Row="0" Grid.Column="0" Content="GMt (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="0" Grid.Column="1" Name="lblGMT" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="1" Grid.Column="0" Content="Displacement (T)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="1" Grid.Column="1" Name="lblDisplacement" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
												</Grid>
											</StackPanel>
										</Border>
									</Grid>

									<!-- Row 2: Drafts & Trim -->
									<Grid Grid.Row="1">
										<Grid.RowDefinitions>
											<RowDefinition Height="Auto"/>
										</Grid.RowDefinitions>
										<!-- Drafts & Trim Box -->
										<Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,0,0,6">
											<Border.Effect>
												<DropShadowEffect ShadowDepth="0" BlurRadius="0.5" Opacity="0.008" Color="#030032"/>
											</Border.Effect>
											<StackPanel>
												<Border Background="{DynamicResource PrimaryBrush}" CornerRadius="6" Padding="10,5" Margin="0,0,0,8" HorizontalAlignment="Stretch">
													<TextBlock Text="Drafts &amp; Trim" FontWeight="Bold" Foreground="White" FontSize="12" Margin="2,0,0,0"/>
												</Border>
												<Grid Margin="2,0,2,0">
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="*"/>
														<ColumnDefinition Width="118"/>
														<ColumnDefinition Width="54"/>
													</Grid.ColumnDefinitions>
													<Grid.RowDefinitions>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="12"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="12"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
													</Grid.RowDefinitions>
													<Label Grid.Row="0" Grid.Column="0" Content="Trim (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="0" Grid.Column="1" Name="lblTrim" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="0" Grid.Column="2" Name="label5" Content="FWD" Foreground="#10B981" FontWeight="ExtraBold" VerticalAlignment="Center" Margin="12,0,0,0"/>
													<Label Grid.Row="1" Grid.Column="0" Content="List (deg.)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="1" Grid.Column="1" Name="lblHeel" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="1" Grid.Column="2" Name="label8" Content="STBD" Foreground="#10B981" FontWeight="ExtraBold" VerticalAlignment="Center" Margin="12,0,0,0"/>
													<Border Grid.Row="2" Grid.ColumnSpan="3" Background="{DynamicResource BorderBrush}" Height="1" Margin="0,4,0,4"/>
													<Label Grid.Row="3" Grid.Column="0" Content="AP (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="3" Grid.Column="1" Name="lblDraftAP" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="4" Grid.Column="0" Content="FP (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="4" Grid.Column="1" Name="lblDraftFP" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="5" Grid.Column="0" Content="Mid (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="5" Grid.Column="1" Name="lblDraftMean" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Border Grid.Row="6" Grid.ColumnSpan="3" Background="{DynamicResource BorderBrush}" Height="1" Margin="0,4,0,4"/>
													<Label Grid.Row="7" Grid.Column="0" Content="Aft Mark (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="7" Grid.Column="1" Name="lblDraftAftMark" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="8" Grid.Column="0" Content="Fwd Mark (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="8" Grid.Column="1" Name="lblDraftFwdMark" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="9" Grid.Column="0" Content="Propeller (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="9" Grid.Column="1" Name="lblPROPELLER" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="10" Grid.Column="0" Content="Sonar Dome (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="10" Grid.Column="1" Name="lblSONARDOME" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
												</Grid>
											</StackPanel>
										</Border>
									</Grid>

									<!-- Row 3: Hydrostatics -->
									<Grid Grid.Row="2">
										<Grid.RowDefinitions>
											<RowDefinition Height="Auto"/>
										</Grid.RowDefinitions>
										<!-- Hydrostatics Box -->
										<Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10,8">
											<Border.Effect>
												<DropShadowEffect ShadowDepth="0" BlurRadius="0.5" Opacity="0.008" Color="#030032"/>
											</Border.Effect>
											<StackPanel>
												<Border Background="{DynamicResource PrimaryBrush}" CornerRadius="6" Padding="10,5" Margin="0,0,0,8" HorizontalAlignment="Stretch">
													<TextBlock Text="Hydrostatics" FontWeight="Bold" Foreground="White" FontSize="12" Margin="2,0,0,0"/>
												</Border>
												<Grid Margin="2,0,2,0">
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="*"/>
														<ColumnDefinition Width="118"/>
														<ColumnDefinition Width="54"/>
													</Grid.ColumnDefinitions>
													<Grid.RowDefinitions>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
														<RowDefinition Height="Auto"/>
													</Grid.RowDefinitions>
													<Label Grid.Row="0" Grid.Column="0" Content="KG (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="0" Grid.Column="1" Name="lblKG" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="1" Grid.Column="0" Content="KG (Fluid)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="1" Grid.Column="1" Name="lblKGF" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="2" Grid.Column="0" Content="LCG (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="2" Grid.Column="1" Name="lblLCG" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="3" Grid.Column="0" Content="FSC (m)" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="3" Grid.Column="1" Name="lblFSC" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="4" Grid.Column="0" Content="TPC" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="4" Grid.Column="1" Name="lblTPC" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
													<Label Grid.Row="5" Grid.Column="0" Content="MTC" Style="{StaticResource HeaderLabel}"/>
													<Label Grid.Row="5" Grid.Column="1" Name="lblMTC" Content="0" Style="{StaticResource ValueLabel}" Margin="0,2"/>
												</Grid>
											</StackPanel>
										</Border>
									</Grid>
								</Grid>
							</StackPanel>
						</ScrollViewer>
					</Grid>
				</TabItem>
	</TabControl>
</Border>

	<!-- ================= BOTTOM PANEL: TANKS ================= -->
		<Border Grid.Row="2" Grid.Column="0" Margin="4,0,6,5" Background="{DynamicResource CardBgBrush}" CornerRadius="12" BorderBrush="#F59E0B" BorderThickness="1.5">
            <Border.Effect>
				<DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.03" Color="#030032"/>
            </Border.Effect>
            <TabControl x:Name="tabControlTankTypesReal" Background="Transparent" BorderThickness="0" TabStripPlacement="Top">
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource TankPanelTabItemStyle}"/>
                </TabControl.Resources>
                <TabControl.Template>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="Transparent">
                                <DockPanel LastChildFill="True">
                                    <Button DockPanel.Dock="Right"
                                            Content="Clear"
                                            Name="btnclearTankTabsReal"
                                            Click="btnclear_Click"
                                            Height="34"
                                            MinWidth="120"
                                            Margin="10,6,8,0"
                                            Padding="18,0"
                                            Background="#1636CC"
                                            Foreground="White"
                                            BorderBrush="#7EA2FF"
                                            BorderThickness="1"
                                            FontWeight="Bold"
                                            Cursor="Hand"/>
                                    <TabPanel x:Name="HeaderPanel"
                                              IsItemsHost="True"
                                              Margin="8,0,0,0"
                                              Background="Transparent"
                                              KeyboardNavigation.TabIndex="1"/>
                                </DockPanel>
                            </Border>
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Grid.Row="1"
                                              Margin="{TemplateBinding Padding}"
                                              ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </TabControl.Template>

                <TabItem Header="Cargo Tanks" Style="{StaticResource CargoUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFrameBlue}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgCargoTanks"
                                      Style="{StaticResource TankTableGridStyle}"
                                      ColumnHeaderStyle="{StaticResource TankTableColumnHeaderStyle}"
                                      CanUserAddRows="False"
                                      CanUserResizeColumns="False"
                                      CanUserReorderColumns="False"
                                      CanUserSortColumns="False"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgTankCompartment_PreviewTextInput"
									  BeginningEdit="dgTankCompartment_BeginningEdit"
									  PreparingCellForEdit="dgTankCompartment_PreparingCellForEdit"
									  CurrentCellChanged="dgTankCompartment_CurrentCellChanged"
									  SelectionUnit="FullRow"
									  ColumnWidth="*"
									  HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="TextAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGrid.Resources>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="TANK NAME" Binding="{Binding Path=Tank_Name}" Width="1.25*" MinWidth="120" IsReadOnly="True" ElementStyle="{StaticResource TankLeftText}" ClipboardContentBinding="{Binding}"/>
                                    <DataGridTextColumn Header="STATUS" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource TankRightText}" Binding="{Binding Path=StatusText}" Width="80" MinWidth="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Mass (T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource TankRightText}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Volume (cu.m)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource TankRightText}" Binding="{Binding Path=Volume,StringFormat={}{0:0.00}}" Width="*" MinWidth="95" IsReadOnly="False"/>
                                    <DataGridTextColumn Header="Percent Fill" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource TankRightText}" Binding="{Binding Path=Percent_Full,StringFormat={}{0:0.00}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Specific Gravity" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource TankRightText}" Binding="{Binding Path=SG,StringFormat={}{0:0.000}}" Width="*" MinWidth="95" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM Type" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource TankRightText}" Binding="{Binding Path=FSMTypeText}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM (T-m)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource TankRightText}" Binding="{Binding Path=FSM,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                </DataGrid.Columns>
                                <DataGrid.RowStyle>
                                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource ModernRowStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="1">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="2">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="Ballast Tanks" Style="{StaticResource BallastUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFrameGreen}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgBallastTanks" Style="{StaticResource TankTableGridStyle}"
                                      ColumnHeaderStyle="{StaticResource TankTableColumnHeaderStyle}"
                                      CanUserAddRows="False"
                                      CanUserResizeColumns="False"
                                      CanUserReorderColumns="False"
                                      CanUserSortColumns="False"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgTankCompartment_PreviewTextInput"
                                      BeginningEdit="dgTankCompartment_BeginningEdit"
                                      PreparingCellForEdit="dgTankCompartment_PreparingCellForEdit"
                                      CurrentCellChanged="dgTankCompartment_CurrentCellChanged"
                                      LoadingRow="dgBallastTanks_LoadingRow"
                                      SelectionUnit="FullRow"
                                      ColumnWidth="*"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="TextAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGrid.Resources>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="TANK NAME" Binding="{Binding Path=Tank_Name}" Width="1.25*" MinWidth="120" IsReadOnly="True" ElementStyle="{StaticResource CenterText}" ClipboardContentBinding="{Binding}"/>
                                    <DataGridTextColumn Header="STATUS" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=StatusText}" Width="80" MinWidth="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="MASS(T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="VOLUME (CU.M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Volume,StringFormat={}{0:0.00}}" Width="*" MinWidth="95" IsReadOnly="False"/>
                                    <DataGridTextColumn Header="PERCENT FILL" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Percent_Full,StringFormat={}{0:0.00}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="S.GRAVITY" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=SG,StringFormat={}{0:0.000}}" Width="*" MinWidth="90" IsReadOnly="True"/>
									<DataGridTextColumn Header="FSM TYPE" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSMTypeText}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM(T-M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSM,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                </DataGrid.Columns>
                                <DataGrid.RowStyle>
                                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource ModernRowStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="1">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="2">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="Fuel Oil Tanks" Style="{StaticResource FuelUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFrameOrange}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgFuelOilTanks" Style="{StaticResource TankTableGridStyle}"
                                      ColumnHeaderStyle="{StaticResource TankTableColumnHeaderStyle}"
                                      CanUserAddRows="False"
                                      CanUserResizeColumns="False"
                                      CanUserReorderColumns="False"
                                      CanUserSortColumns="False"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgTankCompartment_PreviewTextInput"
                                      PreparingCellForEdit="dgTankCompartment_PreparingCellForEdit"
                                      CurrentCellChanged="dgTankCompartment_CurrentCellChanged"
                                      BeginningEdit="dgTankCompartment_BeginningEdit"
                                      LoadingRow="dgFuelOilTanks_LoadingRow"
                                      SelectionUnit="FullRow"
                                      ColumnWidth="*"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="TextAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGrid.Resources>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="TANK NAME" Binding="{Binding Path=Tank_Name}" Width="1.25*" MinWidth="120" IsReadOnly="True" ElementStyle="{StaticResource CenterCellStyle}"/>
									<DataGridTextColumn Header="STATUS" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=StatusText}" Width="80" MinWidth="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="MASS(T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="VOLUME(CU.M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Volume,StringFormat={}{0:0.00}}" Width="*" MinWidth="95" IsReadOnly="False"/>
                                    <DataGridTextColumn Header="PERCENT FILL" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Percent_Full,StringFormat={}{0:0.00}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="S.GRAVITY" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=SG,StringFormat={}{0:0.000}}" Width="*" MinWidth="90" IsReadOnly="True"/>
									<DataGridTextColumn Header="FSM TYPE" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSMTypeText}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM(T-M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSM,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                </DataGrid.Columns>
                                <DataGrid.RowStyle>
                                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource ModernRowStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="1">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="2">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="Fresh Water Tanks" Style="{StaticResource FreshUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFrameBlue}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgFreshWaterTanks" Style="{StaticResource TankTableGridStyle}"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgTankCompartment_PreviewTextInput"
                                      BeginningEdit="dgTankCompartment_BeginningEdit"
                                      PreparingCellForEdit="dgTankCompartment_PreparingCellForEdit"
                                      CurrentCellChanged="dgTankCompartment_CurrentCellChanged"
                                      LoadingRow="dgFreshWaterTanks_LoadingRow"
                                      SelectionUnit="FullRow"
                                      ColumnWidth="*"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="TANK NAME" Binding="{Binding Path=Tank_Name}" Width="1.25*" MinWidth="120" IsReadOnly="True" ElementStyle="{StaticResource CenterCellStyle}"/>
                                    <DataGridTextColumn Header="STATUS" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=StatusText}" Width="80" MinWidth="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="MASS(T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="VOLUME(CU.M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Volume,StringFormat={}{0:0.00}}" Width="*" MinWidth="95" IsReadOnly="False"/>
                                    <DataGridTextColumn Header="PERCENT FILL" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Percent_Full,StringFormat={}{0:0.00}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="S.GRAVITY" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=SG,StringFormat={}{0:0.000}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM TYPE" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSMTypeText}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM(T-M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSM,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                </DataGrid.Columns>
                                <DataGrid.RowStyle>
                                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource ModernRowStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="1">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="2">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="Miscellaneous Tanks" Style="{StaticResource MiscUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFramePurple}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgMiscTanks" Style="{StaticResource TankTableGridStyle}"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgTankCompartment_PreviewTextInput"
                                      BeginningEdit="dgTankCompartment_BeginningEdit"
                                      PreparingCellForEdit="dgTankCompartment_PreparingCellForEdit"
                                      CurrentCellChanged="dgTankCompartment_CurrentCellChanged"
                                      LoadingRow="dgMiscTanks_LoadingRow"
                                      SelectionUnit="FullRow"
                                      ColumnWidth="*"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="TANK NAME" Binding="{Binding Path=Tank_Name}" Width="1.25*" MinWidth="120" IsReadOnly="True" ElementStyle="{StaticResource CenterCellStyle}"/>
                                    <DataGridTextColumn Header="STATUS" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=StatusText}" Width="80" MinWidth="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="MASS(T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="VOLUME(CU.M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Volume,StringFormat={}{0:0.00}}" Width="*" MinWidth="95" IsReadOnly="False"/>
                                    <DataGridTextColumn Header="PERCENT FILL" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Percent_Full,StringFormat={}{0:0.00}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="S.GRAVITY" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=SG,StringFormat={}{0:0.000}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM TYPE" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSMTypeText}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM(T-M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSM,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                </DataGrid.Columns>
                                <DataGrid.RowStyle>
                                    <Style TargetType="{x:Type DataGridRow}" BasedOn="{StaticResource ModernRowStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="1">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Status}" Value="2">
                                                <Setter Property="Background" Value="#FEF2F2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="Non Tight" Style="{StaticResource NonTightUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFrameSlate}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgCompartments" Style="{StaticResource TankTableGridStyle}"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgTankCompartment_PreviewTextInput"
                                      BeginningEdit="dgTankCompartment_BeginningEdit"
                                      PreparingCellForEdit="dgTankCompartment_PreparingCellForEdit"
                                      CurrentCellChanged="dgTankCompartment_CurrentCellChanged"
                                      LoadingRow="dgCompartments_LoadingRow"
                                      SelectionUnit="FullRow"
                                      ColumnWidth="*"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="COMPARTMENT NAME" Binding="{Binding Path=Tank_Name}" Width="1.35*" MinWidth="150" IsReadOnly="True" ElementStyle="{StaticResource CenterCellStyle}"/>
                                    <DataGridTextColumn Header="STATUS" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=StatusText}" Width="80" MinWidth="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="INNAGE (MM)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Sounding_Level,StringFormat={}{0:0.000}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="MASS(T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="VOLUME(CU.M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Volume,StringFormat={}{0:0.00}}" Width="*" MinWidth="95" IsReadOnly="False"/>
                                    <DataGridTextColumn Header="PERCENT FILL" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Percent_Full,StringFormat={}{0:0.00}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="LCG(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=LCG,StringFormat={}{0:0.000}}" Width="*" MinWidth="75" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="VCG(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=VCG,StringFormat={}{0:0.000}}" Width="*" MinWidth="75" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="TCG(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=TCG,StringFormat={}{0:0.000}}" Width="*" MinWidth="75" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM(T-M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSM,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="Water Tight" Style="{StaticResource WaterTightUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFrameBlue}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgWaterTightRegion" Style="{StaticResource TankTableGridStyle}"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgTankCompartment_PreviewTextInput"
                                      BeginningEdit="dgTankCompartment_BeginningEdit"
                                      PreparingCellForEdit="dgTankCompartment_PreparingCellForEdit"
                                      CurrentCellChanged="dgTankCompartment_CurrentCellChanged"
                                      LoadingRow="dgWaterTightRegion_LoadingRow"
                                      SelectionUnit="FullRow"
                                      ColumnWidth="*"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="WT REGION NAME" Binding="{Binding Path=Tank_Name}" Width="1.35*" MinWidth="150" IsReadOnly="True" ElementStyle="{StaticResource CenterCellStyle}"/>
                                    <DataGridTextColumn Header="STATUS" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=StatusText}" Width="80" MinWidth="80" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="INNAGE(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Sounding_Level,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="MASS(T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="VOLUME(CU.M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Volume,StringFormat={}{0:0.00}}" Width="*" MinWidth="95" IsReadOnly="False"/>
                                    <DataGridTextColumn Header="PERCENT FILL" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Percent_Full,StringFormat={}{0:0.00}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="S.GRAVITY" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=SG,StringFormat={}{0:0.000}}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM TYPE" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSMTypeText}" Width="*" MinWidth="90" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FSM(T-M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FSM,StringFormat={}{0:0.000}}" Width="*" MinWidth="85" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="FLOOD RATE(TPH)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=FloodRate,StringFormat={}{0:0.000}}" Width="*" MinWidth="105" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="DWT Constant" Style="{StaticResource DwtConstantUnderlineTabItemStyle}">
                    <Border Style="{StaticResource SectionFramePurple}" Background="{DynamicResource CardBgBrush}" CornerRadius="10" Padding="7" Margin="8,6,8,8">
                        <Grid>
                            <DataGrid Name="dgVariableItems" Style="{StaticResource TankTableGridStyle}"
                                      AutoGenerateColumns="False"
                                      CanUserResizeRows="False"
                                      PreviewTextInput="dgVariableItems_PreviewTextInput"
                                      BeginningEdit="dgVariableItems_BeginningEdit"
                                      PreparingCellForEdit="dgVariableItems_PreparingCellForEdit"
                                      CurrentCellChanged="dgVariableItems_CurrentCellChanged"
                                      SelectionChanged="dgVariableItems_SelectionChanged"
                                      LoadingRow="dgVariableItems_LoadingRow"
                                      KeyDown="dgVariableItems_KeyDown"
                                      SelectionUnit="FullRow"
                                      ColumnWidth="*"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Item Name" Binding="{Binding Path=Load_Name}" Width="1.35*" MinWidth="140" ElementStyle="{StaticResource CenterCellStyle}"/>
                                    <DataGridTextColumn Header="WEIGHT(T)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Weight,StringFormat={}{0:0.00}}" Width="*" MinWidth="85"/>
                                    <DataGridTextColumn Header="LCG(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=LCG,StringFormat={}{0:0.000}}" Width="*" MinWidth="75"/>
                                    <DataGridTextColumn Header="VCG(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=VCG,StringFormat={}{0:0.000}}" Width="*" MinWidth="75"/>
                                    <DataGridTextColumn Header="TCG(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=TCG,StringFormat={}{0:0.000}}" Width="*" MinWidth="75"/>
                                    <DataGridTextColumn Header="LENGTH(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Length,StringFormat={}{0:0.000}}" Width="*" MinWidth="85"/>
                                    <DataGridTextColumn Header="BREADTH(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Breadth,StringFormat={}{0:0.000}}" Width="*" MinWidth="90"/>
                                    <DataGridTextColumn Header="DEPTH(M)" HeaderStyle="{StaticResource DataGridSimulation}" ElementStyle="{StaticResource CenterCellStyle}" Binding="{Binding Path=Depth,StringFormat={}{0:0.000}}" Width="*" MinWidth="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>
            </TabControl>
		</Border>
	</Grid>
</Page>

